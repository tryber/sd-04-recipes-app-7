import React, { /* Component */ } from 'react';
import BottomBar from '../components/BottomBar/bottomBar';

const Explorar = () => (
  <footer>
    <div>Explorar Page</div>
    <BottomBar />
  </footer>
);

export default Explorar;
